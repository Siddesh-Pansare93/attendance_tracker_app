// Basic test for Smart Attendance App
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should initialize', (WidgetTester tester) async {
    // Basic test to verify the app can be imported
    expect(1 + 1, equals(2));
  });
}
